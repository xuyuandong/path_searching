#ifndef DISTANCE_H
#define DISTANCE_H

#include<iostream>
using namespace std;

class Distance
{
 public:
  int index;
  int length;
  int pre;

/*  bool operater < (const Distance &arg)
  {
    return (this.length<arg.length);
  }
 
  bool operater > (const Distance &arg)
  {
    return (this.length>arg.length);
  }


  bool operater <= (const Distance &arg)
  {
    return (this.length<=arg.length);
  }


  bool operater >= (const Distance &arg)
  {
    return (this.length>=arg.length);
  }


  bool operater == (const Distance &arg)
  {
    return (this.length==arg.length);
  }*/
};
#endif
